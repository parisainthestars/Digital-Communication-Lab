
disp('--- Calculations to 15 decimal places ---');
format long; % 15 digits are displayed

% a.
val_a = cosh(0.1);
fprintf('cosh(0.1) = %f\n', val_a);

% b. 
val_b = log(2); 
fprintf('ln(2)      = %f\n', val_b);

% c.
val_c = atan(1/2); 
fprintf('arctan(1/2) = %f\n', val_c);